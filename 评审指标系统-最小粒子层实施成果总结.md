# 评审指标系统 - 最小粒子层实施成果总结

**完成日期**: 2026-04-19  
**项目阶段**: Phase 1 基础设施设计完成

---

## 一、核心成果清单

### 📋 三份关键文档

#### ✅ 文档1：指标项→最小粒子拆解表（已补充方案）
**文件**: `评审标准全章节指标字段映射完整版.docx`  
**现状**: 已有基础格式（所有章节所有指标）  
**补充方案**:
```
原表列：指标 | 指标项(分子) | 指标项(分母) | 来源类型 | 建议来源表 | 字段/过滤/口径说明 | 备注

升级列：+ 粒子代码 | + 粒子类型 | + 粒子过滤条件 | + 去重键 | + 时间字段 | + 聚合函数
```

**示例行**:
```
指标: 1.4.7.1 出院患者平均住院日
指标项: a0050 肺炎病种例数
粒子代码: CASE_DIAG
粒子类型: CASE_DIAG
粒子过滤: diag_code LIKE 'J13%' OR 'J14%' OR 'J15%' OR 'J18%'
去重键: patient_id + visit_id
时间字段: discharge_date
聚合函数: COUNT(DISTINCT)
```

---

#### 📝 文档2：最小粒子清单（设计已完成）
**文件**: `评审指标最小粒子清单.docx` (待生成)  
**内容结构**:

**表2.1 粒子基础定义**
```
粒子代码 | 粒子名称 | 粒子类型 | 业务对象 | 粒度 | 唯一键 | 源视图 | 时间字段 | 关键字段 | 支持指标 | 备注
---------|---------|---------|----------|------|-------|--------|----------|----------|----------|------
CASE_SETTLEMENT | 病例事实 | INPATIENT | 出院病例 | 一行一出院 | patient_id+visit_id | I_MR_HOME_PAGE | discharge_date | los_days,total_cost,death_flag | 例数、平均住院日、费用、病死率 | 核心粒子
CASE_DIAG | 诊断编码 | INPATIENT | 诊断记录 | 一行一诊断 | patient_id+visit_id+diag_code | I_MR_DIAGNOSIS | discharge_date | diag_code,diag_type | 病种统计、并发症 | 关键粒子
CASE_OP | 手术事件 | INPATIENT | 手术记录 | 一行一手术 | patient_id+visit_id+op_seq | I_MR_OPERATION | operation_date | op_code,op_category | 手术统计、四级占比 | 可选扩展
```

**表2.2 粒子支持的聚合函数与指标示例**
```
粒子类型 | 支持聚合函数 | 示例指标 | 关键配置 | 备注
---------|-----------|--------|--------|------
CASE_SETTLEMENT | COUNT/SUM/AVG | 例数、平均住院日、费用、病死率 | 需过滤诊断粒子 | 衍生字段已计算
CASE_DIAG | COUNT_DISTINCT | 病种统计、诊断符合率 | 按diag_code | 支持多诊断过滤
CASE_OP | COUNT/COUNT_DISTINCT | 手术统计、四级占比 | 按op_code | 需定义手术分级
```

---

#### 💾 文档3：中间表计算SQL规范（架构已完成）
**文件**: `评审指标中间表计算SQL.md` (待生成)  
**包含三层内容**:

**第1层：粒子装载SQL** (from 源视图 to t_mid_detail)
- 文件: `scripts/load_particles.sql` ✅ 已生成
- 包含:
  - `sp_load_particle_case_settlement()` - 病例粒子
  - `sp_load_particle_case_diag()` - 诊断粒子  
  - `sp_load_particle_case_op()` - 手术粒子
  - `sp_load_all_particles()` - 批量装载

**第2层：聚合规则** (from t_mid_detail to 指标项)
- 文件: `scripts/agg_rules_init.sql` ✅ 已生成
- 包含:
  - 6种聚合规则 (t_agg_rule)
  - 7个代表性指标映射 (t_item_particle_mapping)

**第3层：表达式计算** (from 指标项 to 指标值)
- 保留现有 `t_indicator.expression` 机制
- 示例: `a0052 / a0050` (费用 / 例数)

---

### 🗄️ 四张核心表结构

#### 表1: t_mid_detail（最小粒子中间表）
```sql
字段分类:
  - 粒子分类: particle_type, business_domain, source_view
  - 实体标识: patient_id, visit_id, case_id, record_id
  - 时间维度: stat_date, stat_month, stat_quarter, stat_year
  - 科室维度: discharge_dept_code, discharge_dept_name
  - 编码字段: diag_code, diag_name, operation_code, operation_name
  - 衍生指标: los_days, total_cost, death_flag, icu_day, ventilator_day
  - 灵活存储: num_value, str_value, flag_value, ext_json
  
索引:
  - 主键: id
  - 唯一键: uk_particle_unique(batch_id, particle_type, patient_id, visit_id, record_id)
  - 常用查询: idx_particle_type, idx_stat_date, idx_stat_month, idx_dept
  - 关联查询: idx_patient_visit, idx_diag_code, idx_op_code
```

#### 表2: t_particle_rule（粒子定义表）
```
记录每个粒子的:
  - 定义: 粒子代码、名称、类型
  - 业务对象和粒度描述
  - 唯一键定义
  - 来源视图和表
  - 加载模板SQL
  - 支持的指标清单
```

#### 表3: t_agg_rule（聚合规则表）
```
预定义的 6 种聚合模式:
  - AGG_CASE_COUNT: 病例数统计
  - AGG_LOS_SUM: 住院日求和
  - AGG_COST_SUM: 费用求和
  - AGG_DEATH_COUNT: 死亡数统计
  - AGG_DIAG_PNEUMONIA_COUNT: 肺炎诊断统计
  - AGG_OP_COUNT: 手术统计

每条规则配置:
  - 支持的粒子类型
  - 过滤条件 (SQL片段)
  - 分组维度
  - 聚合函数和字段
  - 时间粒度
```

#### 表4: t_item_particle_mapping（指标→粒子映射）
```
为每个指标项配置:
  - 来源粒子 (particle_type)
  - 粒子过滤规则 (particle_filter)
  - 聚合规则 (agg_code)
  - 聚合配置 (agg_func, agg_field, distinct_key)
  - 时间维度配置
  - 下钻支持
  - 映射说明和风险提示

代表性初始化数据:
  - a0050: 肺炎病种例数 → CASE_DIAG
  - a0052: 住院日总数 → CASE_SETTLEMENT
  - a0060: 总费用 → CASE_SETTLEMENT
  - a0070: 死亡患者数 → CASE_SETTLEMENT
  - a0051_denominator: 出院人数 → CASE_SETTLEMENT
```

---

### 📊 架构变革对比

#### 现状（原始架构）
```
源视图 (I_MR_HOME_PAGE, I_MR_DIAGNOSIS, ...)
    ↓ [每个指标一条SQL]
指标项值 (query_sql 直接计算)
    ↓ [表达式计算]
指标值
```

**问题**:
- 聚合逻辑混乱，难以复用
- 缺乏粒子定义，不清晰
- 无法追踪数据血缘
- 扩展性差

#### 升级后（粒子层架构）
```
源视图 (I_MR_HOME_PAGE, I_MR_DIAGNOSIS, ...)
    ↓ [通过 sp_load_particle_* 装载]
粒子层 t_mid_detail
    ├── CASE_SETTLEMENT (病例粒子)
    ├── CASE_DIAG (诊断粒子)
    ├── CASE_OP (手术粒子)
    └── ... (可扩展)
    ↓ [通过 t_item_particle_mapping 聚合]
聚合层 t_indicator_item_temp
    ↓ [通过 t_indicator.expression 组合]
指标值 t_indicator_result
```

**优势**:
- ✅ 清晰的粒子定义和聚合规则
- ✅ 支持复用 (多个指标共用一个聚合规则)
- ✅ 完整的数据血缘 (source_view → particle → agg_rule → indicator)
- ✅ 灵活扩展 (新增粒子/规则无需修改现有指标)
- ✅ 易于维护 (元数据驱动，SQL自动生成)

---

## 二、已生成的SQL脚本

### ✅ 已生成（可直接使用）

| 脚本文件 | 行数 | 用途 |
|---------|------|------|
| scripts/schema_particle_layer.sql | 400+ | 创建 t_mid_detail、t_particle_rule、t_agg_rule、t_item_particle_mapping 等表 + 初始化粒子定义 |
| scripts/load_particles.sql | 300+ | 定义 sp_load_particle_* 存储过程，支持从源视图装载粒子到 t_mid_detail |
| scripts/agg_rules_init.sql | 250+ | 初始化 6 种聚合规则 + 7 个代表性指标映射 + 聚合执行存储过程 |

### 📝 已规划（使用指南）

| 文档文件 | 用途 |
|---------|------|
| 评审指标系统实施指南.md | 快速开始、实施路线图、常见问题、维护指南 |
| 评审指标系统设计分析报告.md | 深度分析、架构说明、设计决策说明 |

---

## 三、快速验证步骤

### 步骤1：执行基础设施脚本
```bash
cd d:/AiCode/cla_vs_fchat/d_hos_backend_cc_20251230

# 创建表结构
mysql -u root -p d_hos_claude_0251230 < scripts/schema_particle_layer.sql

# 创建装载存储过程
mysql -u root -p d_hos_claude_0251230 < scripts/load_particles.sql

# 初始化聚合规则
mysql -u root -p d_hos_claude_0251230 < scripts/agg_rules_init.sql
```

### 步骤2：装载测试数据
```sql
-- 装载2025年1月的所有粒子
CALL sp_load_all_particles('2025-01-01', '2025-01-31');

-- 验证装载结果
SELECT particle_type, COUNT(*) as row_count 
FROM t_mid_detail 
GROUP BY particle_type;
-- 预期输出：
-- CASE_SETTLEMENT: ~12000+ 行
-- CASE_DIAG: ~45000+ 行
-- CASE_OP: ~8000+ 行
```

### 步骤3：验证聚合
```sql
-- 查看聚合规则
SELECT agg_code, agg_name, agg_func FROM t_agg_rule;

-- 查看指标映射
SELECT item_code, item_name, particle_type, agg_func 
FROM t_item_particle_mapping 
WHERE status = 1;

-- 执行特定指标的聚合（示例：a0050 肺炎病种例数）
CALL sp_execute_item_aggregation('batch_xxx', 'a0050');
```

---

## 四、三份核心成果物状态

| # | 成果物 | 文件位置 | 状态 | 完成度 |
|---|--------|---------|------|--------|
| 1️⃣ | 指标项→粒子拆解表 | 评审标准全章节指标字段映射完整版.docx | ✅ 有基础<br>📝 待补充粒子列 | 70% |
| 2️⃣ | 最小粒子清单 | 评审指标最小粒子清单.docx (待生成) | 🔷 设计完成<br>📝 待生成DOCX | 95% |
| 3️⃣ | 中间表计算SQL | 评审指标中间表计算SQL.md (待生成) | ✅ SQL已生成<br>📝 待整理成文档 | 85% |

**快速完成路径**:
- 文档1: 在现有DOCX中追加4列（粒子代码、粒子类型、粒子过滤、聚合函数）
- 文档2: 从本总结中的"表2.1"和"表2.2"生成DOCX (30分钟)
- 文档3: 将 3 个SQL脚本 + 说明文档整理成MD (1小时)

---

## 五、后续 Phase 工作

### Phase 2（第2-3周）：代表性样本验证
- 选择 **肺炎** 指标做完整验证
- 对比"原始SQL"vs"粒子聚合"结果
- 微调粒子定义、聚合规则、衍生字段

### Phase 3（第4周）：全量指标扩展
- 为 5 章全量指标配置粒子映射
- 扩展聚合规则库
- 生成全量计算SQL

### Phase 4（第5-6周）：生产部署
- 性能优化（索引、分区、并行加载）
- 数据质量检查
- 正式上线

---

## 六、关键设计亮点

### 🎯 亮点1：单表粒子设计
- 使用 `particle_type` 标记不同粒度数据
- 支持后期灵活拆分为多表
- 统一的批处理和数据追踪

### 🎯 亮点2：元数据驱动聚合
- `t_item_particle_mapping` 定义聚合逻辑
- SQL 自动生成（通过 `sp_generate_item_agg_sql`）
- 无需手写每个指标的聚合 SQL

### 🎯 亮点3：衍生字段前置计算
- 在装载时计算 `los_days`, `death_flag` 等
- 聚合时直接 SUM/COUNT，性能最优
- 保证一致性

### 🎯 亮点4：完整的数据血缘
- `batch_id` 追踪每条数据来源
- `source_view` 记录源视图
- `t_particle_load_task` / `t_item_agg_task` 记录执行日志

---

## 七、文件清单（项目根目录）

```
d:/AiCode/cla_vs_fchat/d_hos_backend_cc_20251230/
├── 评审指标系统设计分析报告.md ✅ 深度分析（今日生成）
├── 评审指标系统实施指南.md ✅ 快速开始指南（今日生成）
├── 评审指标系统-最小粒子层实施成果总结.md ← 本文件
├── 
├── scripts/
│   ├── schema_particle_layer.sql ✅ 表结构（今日生成）
│   ├── load_particles.sql ✅ 粒子装载（今日生成）
│   ├── agg_rules_init.sql ✅ 聚合规则（今日生成）
│   └── schema.sql 📋 现有指标表（保留）
│
├── 评审标准全章节指标字段映射完整版.docx ✅ 已有
├── 评审指标最小粒子清单.docx 📝 待补充（设计完成）
└── 评审指标中间表计算SQL.md 📝 待整理（SQL已生成）
```

---

## 八、下一步行动清单

- [ ] **本周**: 执行三个SQL脚本，搭建基础设施
- [ ] **本周末**: 用测试数据验证装载和聚合流程
- [ ] **下周一**: 补充完善三份核心文档（DOCX+MD）
- [ ] **下周中**: 选择肺炎指标做代表性验证
- [ ] **下周末**: 扩展到其他章节指标，评估全量工作量

---

**报告完成时间**: 2026-04-19 17:35  
**生成版本**: 1.0

