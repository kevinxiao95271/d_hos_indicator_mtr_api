# 评审指标系统 - 最小粒子层实施指南

**版本**: 1.0  
**日期**: 2026-04-19  
**目标**: 从现有指标系统升级到粒子层架构

---

## 一、核心交付物（三大成果）

### 1.1 第一份：指标项→最小粒子拆解表（已有）

**文件**: `评审标准全章节指标字段映射完整版.docx`  
**格式**: 表格形式，按章节展示所有指标

**补充方案**:  
在现有表格基础上，为每个指标项增加粒子配置列：

```
原表：
| 指标 | 指标项(分子) | 指标项(分母) | 来源类型 | 建议来源表 | 字段/过滤/口径说明 | 备注 |

升级为：
| 指标 | 指标项(分子) | 指标项(分母) | 粒子代码 | 粒子类型 | 粒子过滤条件 | 去重键 | 时间字段 | 聚合函数 | 备注 |
```

**示例补充**:
| 指标 | 指标项 | 粒子代码 | 粒子类型 | 粒子过滤条件 | 去重键 | 聚合函数 |
|---|---|---|---|---|---|---|
| 1.4.7.1 出院患者平均住院日 | a0050 肺炎病种例数 | CASE_DIAG | CASE_DIAG | ICD-10: J13/J14/J15/J18% | patient_id+visit_id | COUNT_DISTINCT |
| | a0052 住院日总数 | CASE_SETTLEMENT | CASE_SETTLEMENT | los_days > 0 | patient_id+visit_id | SUM(los_days) |

---

### 1.2 第二份：最小粒子清单（新增）

**文件**: `评审指标最小粒子清单.docx`  
**格式**: 粒子定义表 + 用途矩阵

**表1：粒子基础定义**

| 粒子代码 | 粒子名称 | 粒子类型 | 业务对象 | 粒度 | 唯一键 | 源视图 | 时间字段 |
|---|---|---|---|---|---|---|---|
| CASE_SETTLEMENT | 病例事实 | INPATIENT | 出院病例 | 一行一出院 | patient_id+visit_id | I_MR_HOME_PAGE | discharge_date |
| CASE_DIAG | 诊断编码 | INPATIENT | 诊断记录 | 一行一诊断 | patient_id+visit_id+diag_code+diag_type | I_MR_DIAGNOSIS | discharge_date |
| CASE_OP | 手术事件 | INPATIENT | 手术记录 | 一行一手术 | patient_id+visit_id+operation_sequence | I_MR_OPERATION | operation_date |

**表2：粒子核心字段**

| 粒子代码 | 核心字段 | 衍生字段 | 支持指标示例 |
|---|---|---|---|
| CASE_SETTLEMENT | patient_id, visit_id, discharge_date, discharge_dept, los_days, total_cost | death_flag, transfer_flag, icu_day | 例数、平均住院日、费用、病死率、转院率 |
| CASE_DIAG | patient_id, visit_id, diag_code, diag_type | diag_name | 病种统计、并发症、诊断符合率 |
| CASE_OP | patient_id, visit_id, operation_code, operation_sequence | operation_category | 手术统计、四级手术占比 |

**表3：指标与粒子支持矩阵**

| 指标编码 | 指标名称 | 支持粒子 | 聚合函数 | 备注 |
|---|---|---|---|---|
| 1.4.7.1 | 出院患者平均住院日 | CASE_SETTLEMENT | SUM/COUNT | 衍生：los_days已计算 |
| 2.1.1 | 收治病种数量 | CASE_DIAG | DISTINCT_COUNT | 按 diag_code 去重 |
| 2.1.10 | 四级手术占比 | CASE_OP | COUNT | 需定义四级手术编码范围 |

---

### 1.3 第三份：中间表计算SQL规范（新增）

**文件**: `评审指标中间表计算SQL.md`  
**内容组成**:

#### Part 1: 粒子装载SQL

**文件**: `scripts/load_particles.sql`  
**包含**:
- `sp_load_particle_case_settlement()` - 病例粒子装载
- `sp_load_particle_case_diag()` - 诊断粒子装载
- `sp_load_particle_case_op()` - 手术粒子装载
- `sp_load_all_particles()` - 批量装载

**执行示例**:
```sql
-- 装载2025年1月的所有粒子
CALL sp_load_all_particles('2025-01-01', '2025-01-31');

-- 查看装载结果
SELECT particle_type, COUNT(*) as row_count 
FROM t_mid_detail 
WHERE batch_id = '20250419120000' 
GROUP BY particle_type;
```

#### Part 2: 聚合规则初始化

**文件**: `scripts/agg_rules_init.sql`  
**包含**:
- `t_agg_rule` - 6种聚合规则
  - AGG_CASE_COUNT: 病例计数
  - AGG_LOS_SUM: 住院日求和
  - AGG_COST_SUM: 费用求和
  - AGG_DEATH_COUNT: 死亡数
  - AGG_DIAG_PNEUMONIA_COUNT: 特定诊断统计
  - AGG_OP_COUNT: 手术统计

- `t_item_particle_mapping` - 代表性指标映射
  - a0050: 肺炎病种例数 → CASE_DIAG + COUNT_DISTINCT
  - a0052: 住院日总数 → CASE_SETTLEMENT + SUM
  - a0051_denominator: 出院人数 → CASE_SETTLEMENT + COUNT
  - a0060: 总费用 → CASE_SETTLEMENT + SUM
  - a0070: 死亡患者数 → CASE_SETTLEMENT + SUM

#### Part 3: 聚合SQL执行

**存储过程**:
- `sp_generate_item_agg_sql()` - 动态生成聚合SQL（用于验证）
- `sp_execute_item_aggregation()` - 执行聚合并保存结果

**执行示例**:
```sql
-- 生成指标项a0050的聚合SQL
CALL sp_generate_item_agg_sql('a0050', 'batch_20250419', 'MONTH');

-- 执行a0050的聚合
CALL sp_execute_item_aggregation('batch_20250419', 'a0050');
```

---

## 二、表结构关键设计决策

### 2.1 为什么是单表 t_mid_detail？

**优势**:
1. **灵活拆分**: 使用 `particle_type` 标记，后续可轻易按粒子类型分表
2. **简化运维**: 统一的批处理、备份、数据清理逻辑
3. **快速验证**: 前期快速铺开，无需频繁改表结构
4. **易于监控**: 单表的数据质量监控、性能调优更直观

**后期拆分计划**:
```
t_mid_detail (现有)
├── t_mid_case_settlement (病例粒子专用表)
├── t_mid_case_diag (诊断粒子专用表)
├── t_mid_case_op (手术粒子专用表)
└── ... (按需扩展)
```

### 2.2 为什么分离聚合层？

**现状**:
- 指标项 SQL 直接从源视图查询，聚合逻辑混乱
- 无法复用相同的聚合规则
- 难以追踪数据血缘

**升级后**:
```
源视图 (I_MR_HOME_PAGE, etc.)
    ↓
粒子层 (t_mid_detail)
    ↓ [通过 t_particle_rule]
聚合层 (t_agg_rule + t_item_particle_mapping)
    ↓ [通过 t_indicator.expression]
指标层 (t_indicator_result)
```

### 2.3 衍生字段在装载时计算的好处

**问题**: 如果在聚合时计算 los_days = discharge_date - admission_date，会导致：
- 每个聚合都重复计算
- 聚合性能下降（需要窗口函数或复杂表达式）

**方案**: 在粒子装载时就计算
```sql
-- 装载时直接计算
los_days = DATEDIFF(discharge_date, admission_date)
death_flag = CASE WHEN discharge_type = '死亡' THEN 1 ELSE 0 END

-- 聚合时直接使用
SUM(los_days)  -- 非常快
SUM(death_flag)  -- 非常快
```

---

## 三、实施路线图（分阶段）

### Phase 1: 基础设施搭建（1-2周）

✅ **已完成**:
- [x] 设计分析报告
- [x] 表结构定义（schema_particle_layer.sql）
- [x] 粒子装载模板（load_particles.sql）
- [x] 聚合规则初始化（agg_rules_init.sql）

⏳ **待执行**:
- [ ] 执行 `schema_particle_layer.sql` 创建表
- [ ] 执行 `load_particles.sql` 创建装载存储过程
- [ ] 执行 `agg_rules_init.sql` 初始化规则和映射

### Phase 2: 代表性样本验证（1周）

选择 **肺炎** 的 4 件套作为验证样本：

1. **病种例数** (a0050)
   - 粒子：CASE_DIAG
   - 过滤：ICD-10 J13/J14/J15/J18%
   - 聚合：COUNT(DISTINCT patient_id, visit_id)

2. **住院日总数** (a0052)
   - 粒子：CASE_SETTLEMENT
   - 过滤：los_days > 0 且存在肺炎诊断
   - 聚合：SUM(los_days)

3. **平均住院日** (计算指标)
   - 表达式：a0052 / a0050

4. **费用统计** (a0060)
   - 粒子：CASE_SETTLEMENT  
   - 过滤：total_cost > 0
   - 聚合：SUM(total_cost)

**验证方法**:
```sql
-- 原始方法（直查源视图）
SELECT COUNT(DISTINCT mr.patient_id)
FROM I_MR_HOME_PAGE mr
WHERE mr.discharge_date BETWEEN '2025-01-01' AND '2025-01-31'
  AND (mr.C03C LIKE 'J13%' OR mr.C03C LIKE 'J14%' ...);

-- 粒子聚合方法
SELECT COUNT(DISTINCT CONCAT(patient_id, '#', visit_id))
FROM t_mid_detail
WHERE batch_id = '20250419120000'
  AND particle_type = 'CASE_DIAG'
  AND (diag_code LIKE 'J13%' OR diag_code LIKE 'J14%' ...);

-- 两个方法结果必须一致 ✓
```

### Phase 3: 文档补充与SQL生成（2周）

- [ ] 补充"指标项→粒子拆解表"的粒子列
- [ ] 生成"最小粒子清单" DOCX
- [ ] 生成"中间表计算SQL规范" MD
- [ ] 为全量代表性指标生成映射数据

### Phase 4: 全量落地（3-4周）

- [ ] 装载全量粒子数据
- [ ] 初始化全量指标的聚合规则
- [ ] 验证全量指标计算结果
- [ ] 部署到生产环境

---

## 四、快速开始（最小可用样本）

### 4.1 执行表创建

```bash
# 在MySQL数据库中执行
mysql -u root -p d_hos_claude_0251230 < scripts/schema_particle_layer.sql
```

### 4.2 创建装载存储过程

```bash
mysql -u root -p d_hos_claude_0251230 < scripts/load_particles.sql
```

### 4.3 初始化聚合规则

```bash
mysql -u root -p d_hos_claude_0251230 < scripts/agg_rules_init.sql
```

### 4.4 装载测试数据

```sql
-- 装载2025年1月的所有粒子
CALL sp_load_all_particles('2025-01-01', '2025-01-31');

-- 查看装载结果
SELECT particle_type, COUNT(*) as row_count, COUNT(DISTINCT batch_id) as batch_count
FROM t_mid_detail
GROUP BY particle_type;

-- 输出示例：
-- CASE_SETTLEMENT | 12345 | 1
-- CASE_DIAG       | 45678 | 1
-- CASE_OP         |  8901 | 1
```

### 4.5 验证聚合

```sql
-- 验证肺炎病种例数
CALL sp_execute_item_aggregation('batch_20250419', 'a0050');

-- 查看聚合结果
SELECT item_code, dept_code, result_value 
FROM t_indicator_item_temp 
WHERE item_code = 'a0050' 
ORDER BY dept_code;
```

---

## 五、后续维护和扩展

### 5.1 增加新的粒子类型

1. 在 `t_particle_rule` 表中新增粒子定义
2. 创建对应的 `sp_load_particle_[type]()` 存储过程
3. 更新 `sp_load_all_particles()` 调用新的加载过程

### 5.2 增加新的聚合规则

1. 在 `t_agg_rule` 表中新增聚合规则
2. 在 `t_item_particle_mapping` 中为新指标项配置映射

### 5.3 性能优化

```sql
-- 定期维护粒子表索引
ANALYZE TABLE t_mid_detail;

-- 删除过期粒子数据（按batch_id）
DELETE FROM t_mid_detail 
WHERE batch_id < DATE_FORMAT(DATE_SUB(NOW(), INTERVAL 3 MONTH), '%Y%m%d%H%i%s');

-- 分区表优化（大数据量时）
ALTER TABLE t_mid_detail PARTITION BY RANGE (YEAR(create_time)) (
    PARTITION p_2024 VALUES LESS THAN (2025),
    PARTITION p_2025 VALUES LESS THAN (2026),
    PARTITION p_2026 VALUES LESS THAN (2027)
);
```

---

## 六、常见问题解答

### Q1: 粒子装载失败怎么办？

A: 检查 `t_particle_load_task` 表中的错误信息：
```sql
SELECT batch_id, particle_type, error_message 
FROM t_particle_load_task 
WHERE task_status = 'FAILED' 
ORDER BY create_time DESC LIMIT 10;
```

### Q2: 聚合结果与原始SQL不一致？

A: 按以下顺序检查：
1. 粒子过滤条件是否正确（`t_item_particle_mapping.particle_filter`）
2. 去重键是否正确（通常是 `patient_id + visit_id`）
3. 衍生字段计算是否正确（如 `los_days` 的计算公式）

### Q3: 如何支持时间对比（同比、环比）？

A: 在 `t_indicator_result` 表中添加字段：
```sql
ALTER TABLE t_indicator_result ADD COLUMN (
    year_over_year DECIMAL(10,4) COMMENT '同比增长率',
    month_over_month DECIMAL(10,4) COMMENT '环比增长率'
);

-- 计算同比
UPDATE t_indicator_result ir1
JOIN t_indicator_result ir2 ON ir1.metric_code = ir2.metric_code
SET ir1.year_over_year = (ir1.result_value - ir2.result_value) / ir2.result_value * 100
WHERE YEAR(ir1.end_date) = YEAR(ir2.end_date) + 1;
```

---

## 七、文件清单

| 文件 | 用途 | 状态 |
|---|---|---|
| 评审指标系统设计分析报告.md | 整体设计说明 | ✅ 已生成 |
| scripts/schema_particle_layer.sql | 表结构定义 | ✅ 已生成 |
| scripts/load_particles.sql | 粒子装载模板 | ✅ 已生成 |
| scripts/agg_rules_init.sql | 聚合规则初始化 | ✅ 已生成 |
| 评审标准全章节指标字段映射完整版.docx | 指标→粒子映射 | ✅ 已有 |
| 评审指标最小粒子清单.docx | 粒子定义清单 | 📝 待补充 |
| 评审指标中间表计算SQL.md | 计算SQL规范 | 📝 待补充 |

---

## 八、下一步行动

1. **本周**: 执行 Phase 1 SQL 脚本，建立基础设施
2. **下周**: 选择肺炎指标做代表性验证（Phase 2）
3. **第3周**: 补充完善三份核心文档
4. **第4周**: 扩展到全量指标，部署生产

---

