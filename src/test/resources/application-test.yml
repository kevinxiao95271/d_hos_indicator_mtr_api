server:
  port: 8075

spring:
  application:
    name: indicator-mtr-test

  # 数据源配置（使用生产数据库，确保测试有真实数据）
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://gz-cdb-bq7gk3k5.sql.tencentcdb.com:63606/d_hos_indicator_mtr_20260720?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=true&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
      username: root
      password: Yiguo9527_

      initial-size: 2
      min-idle: 2
      max-active: 10
      max-wait: 60000

# MyBatis Plus 配置
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  type-aliases-package: com.hospital.indicator.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: AUTO
      table-prefix: t_
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# 日志配置（测试环境打印详细日志）
logging:
  level:
    root: INFO
    com.hospital.indicator: DEBUG
    com.hospital.indicator.mapper: DEBUG
    com.hospital.indicator.service.shard: DEBUG

# 分片任务配置
indicator:
  shard:
    slice:
      timeout-seconds: 60  # 测试环境增加超时时间
