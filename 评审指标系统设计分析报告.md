# 三级医院评审指标系统设计分析报告

**报告日期**: 2026-04-19  
**版本**: 1.0  
**目标**: 从现有 KPI 架构升级到评审指标的最小粒子层设计

---

## 一、现状分析

### 1.1 现有系统架构

#### KPI 系统（来自容器镜像 urm-v8:20260331b）

**表结构**:
- `DATASOURCE`: 数据源定义（表名、参数、字段配置）
- `KPI_ITEM`: 指标项 + 指标项类型（value/ratio/distinct_count）
- `KPI_CATEGORY`: 指标分类
- `KPI_RESULT`: 指标计算结果

**关键特性**:
- 基于 `sp_kpi_item_create` 存储过程的元数据驱动
- 支持参数化数据源（`p_date_type`, `p_dept_code` 等）
- 聚合类型多样: `sum`, `distinct_count`, `count`, `avg`
- 指标体系逻辑清晰：数据源 → KPI_ITEM → KPI_RESULT

**现有指标示例**:
```
1.4.7.1.1 出院患者占用总床日数 (分子)
          来源: 出院患者占用总床日数 数据源
          聚合: SUM
          
1.4.7.1.2 同期出院患者人数 (分母)
          来源: 病案首页明细查询 数据源
          聚合: SUM
          
1.4.7.1 出院患者平均住院日 (比率)
        表达式: a0052 / a0050
```

#### 评审指标系统（用户项目）

**表结构**:
- `t_indicator_item`: 指标项 + SQL 查询
- `t_indicator`: 指标层级树 + 表达式
- `t_indicator_result`: 指标计算结果
- `t_indicator_result_dept`: 科室下钻结果
- `t_indicator_item_temp`: 中间计算结果

**现存问题**:
- query_sql 直接写在表中，缺乏参数化
- 没有最小粒子层，指标和数据源之间耦合
- 聚合逻辑分散在 query_sql 中
- 难以复用 SQL 和聚合规则

---

## 二、评审指标的最小粒子模型设计

### 2.1 三类核心粒子

#### （1）病例粒子 CASE_SETTLEMENT
```
粒度: 一行一个出院病例/一次住院
唯一键: patient_id + visit_no
核心字段:
  - discharge_date: 出院日期 (聚合维度)
  - discharge_dept_code/name: 出院科室 (下钻维度)
  - total_los_days: 住院日数 (衍生)
  - total_cost: 总费用 (衍生)
  - death_flag: 死亡标志 (衍生)
  - disease_code: 诊断编码 (关联诊断粒子)
```

支持指标示例:
- 例数统计: COUNT(DISTINCT case_id)
- 平均住院日: SUM(los_days) / COUNT(DISTINCT case_id)
- 次均费用: SUM(total_cost) / COUNT(DISTINCT case_id)
- 病死率: SUM(death_flag) / COUNT(DISTINCT case_id)

#### （2）诊断粒子 CASE_DIAG
```
粒度: 一行一个病例的一条诊断编码
唯一键: patient_id + visit_no + diagnosis_code + diag_type (主诊/其他)
核心字段:
  - diagnosis_code: ICD-10 编码
  - diagnosis_name: 诊断名称
  - diag_type: 诊断类型 (主诊=1, 其他=2)
  - discharge_date: 出院日期 (关联聚合维度)
  - discharge_dept: 出院科室 (关联下钻维度)
  - disease_category: 病种分类 (如 DRG、学科)
```

支持指标示例:
- 病种命中计数: COUNT(DISTINCT CASE WHEN diagnosis_code LIKE 'J13%' ...)
- 病种例数占比: SUM(CASE WHEN DRG_CODE IN (...) THEN 1 ELSE 0 END) / COUNT(*)
- 并发症发生率: SUM(CASE WHEN disease_category='并发症' THEN 1 ELSE 0 END)

#### （3）手术粒子 CASE_OP (可选扩展)
```
粒度: 一行一次手术事件
唯一键: patient_id + visit_no + operation_sequence_no
核心字段:
  - operation_code: ICD-9-CM-3 编码
  - operation_date: 手术日期
  - operation_category: 手术分类 (如 四级手术)
  - discharge_dept: 所属科室 (与病例关联)
  - surgery_outcome: 手术结果
```

### 2.2 统一中间表设计：t_mid_detail

**表结构**（单表方案，用 particle_type 区分）:

```sql
CREATE TABLE t_mid_detail (
  -- 基础字段
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  batch_id VARCHAR(50) NOT NULL COMMENT '批次ID',
  
  -- 粒子分类
  particle_type VARCHAR(50) NOT NULL COMMENT 'CASE_SETTLEMENT/CASE_DIAG/CASE_OP/...',
  business_domain VARCHAR(50) COMMENT '业务域：INPATIENT/ER/ICU/VTE/INFECT',
  source_view VARCHAR(100) COMMENT '来源视图',
  
  -- 实体标识
  patient_id VARCHAR(50) NOT NULL,
  visit_id VARCHAR(50) NOT NULL,
  case_id VARCHAR(50) COMMENT '出院病例唯一ID',
  record_id VARCHAR(50) COMMENT '粒子特定记录ID(如诊断序号)',
  
  -- 维度字段（用于聚合和下钻）
  stat_date DATE NOT NULL COMMENT '统计日期（如出院日期）',
  stat_month VARCHAR(7) NOT NULL COMMENT '统计月份（YYYY-MM）',
  discharge_dept_code VARCHAR(50) COMMENT '出院科室编码',
  discharge_dept_name VARCHAR(100) COMMENT '出院科室名称',
  
  -- 编码/分类字段
  diag_code VARCHAR(50) COMMENT '诊断编码(CASE_DIAG)',
  diag_name VARCHAR(200) COMMENT '诊断名称',
  operation_code VARCHAR(50) COMMENT '手术编码(CASE_OP)',
  operation_name VARCHAR(200) COMMENT '手术名称',
  
  -- 衍生指标字段（由聚合层计算产生）
  los_days INT COMMENT '住院日数',
  total_cost DECIMAL(15,2) COMMENT '总费用',
  death_flag TINYINT COMMENT '死亡标志(0/1)',
  transfer_flag TINYINT COMMENT '转院标志',
  icu_day INT COMMENT 'ICU住院日(如适用)',
  
  -- 灵活存储
  num_value DECIMAL(15,4) COMMENT '数值',
  str_value VARCHAR(500) COMMENT '字符值',
  flag_value TINYINT COMMENT '标志值',
  ext_json JSON COMMENT '扩展字段(JSON)',
  
  -- 元数据
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  
  -- 索引优化
  KEY idx_batch_id (batch_id),
  KEY idx_particle_type (particle_type),
  KEY idx_stat_date (stat_date),
  KEY idx_stat_month (stat_month),
  KEY idx_dept (discharge_dept_code),
  KEY idx_diag (diag_code),
  KEY idx_patient_visit (patient_id, visit_id),
  UNIQUE KEY uk_particle_unique (batch_id, particle_type, patient_id, visit_id, record_id)
);
```

**粒子分类字段说明**:
| particle_type | 粒度 | 唯一键 | 用途 | 支持聚合 |
|---|---|---|---|---|
| CASE_SETTLEMENT | 一行一出院 | patient_id+visit_id | 病例层统计 | COUNT、SUM |
| CASE_DIAG | 一行一诊断 | patient_id+visit_id+diag_code | 诊断命中、并发症 | COUNT、SUM |
| CASE_OP | 一行一手术 | patient_id+visit_id+op_seq | 手术统计 | COUNT、SUM |
| CASE_VTE | 一行一评估 | patient_id+visit_id+assess_date | VTE风险 | COUNT、SUM |
| CASE_INFECT | 一行一院感 | patient_id+visit_id+infect_date | 院感事件 | COUNT、SUM |

---

## 三、聚合层设计（新增）

### 3.1 聚合规则表：t_agg_rule

```sql
CREATE TABLE t_agg_rule (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  agg_code VARCHAR(50) UNIQUE NOT NULL COMMENT '聚合规则编码',
  agg_name VARCHAR(200) COMMENT '聚合规则名称',
  
  -- 粒子配置
  particle_types VARCHAR(500) COMMENT '支持的粒子类型(JSON数组)',
  filter_condition VARCHAR(1000) COMMENT '过滤条件(SQL片段)',
  
  -- 分组维度
  group_by_fields VARCHAR(500) COMMENT '分组字段(JSON数组)',
  
  -- 聚合函数
  agg_func VARCHAR(50) COMMENT 'COUNT/SUM/AVG/DISTINCT_COUNT/...',
  agg_field VARCHAR(100) COMMENT '聚合字段(如 id 做 COUNT)',
  
  -- 去重
  distinct_key VARCHAR(500) COMMENT '去重字段(JSON数组)',
  
  -- 时间维度
  time_dimension VARCHAR(20) COMMENT 'YEAR/QUARTER/MONTH/DAY',
  
  -- 结果映射
  result_field VARCHAR(100) COMMENT '结果存储字段名',
  result_unit VARCHAR(50) COMMENT '结果单位',
  
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY uk_agg_code (agg_code)
);
```

### 3.2 指标项与粒子映射表：t_item_particle_mapping

```sql
CREATE TABLE t_item_particle_mapping (
  id BIGINT AUTO_INCREMENT PRIMARY KEY,
  item_code VARCHAR(50) NOT NULL COMMENT '指标项编码(如 a0050)',
  item_name VARCHAR(200) COMMENT '指标项名称',
  
  -- 粒子配置
  particle_type VARCHAR(50) COMMENT 'CASE_SETTLEMENT/CASE_DIAG/...',
  particle_filter VARCHAR(500) COMMENT '粒子过滤规则',
  
  -- 聚合配置
  agg_code VARCHAR(50) COMMENT '聚合规则编码',
  agg_func VARCHAR(50) COMMENT '聚合函数',
  distinct_key VARCHAR(500) COMMENT '去重键',
  
  -- 时间和维度
  time_field VARCHAR(100) COMMENT '时间字段名',
  time_dimension VARCHAR(20) COMMENT '统计粒度',
  group_by_fields VARCHAR(500) COMMENT '分组维度',
  
  -- 结果配置
  result_field VARCHAR(100) COMMENT '结果字段',
  unit VARCHAR(50) COMMENT '单位',
  
  -- 备注
  notes TEXT COMMENT '映射说明',
  risks TEXT COMMENT '风险点',
  
  UNIQUE KEY uk_item_code (item_code)
);
```

---

## 四、SQL 计算链路升级

### 4.1 粒子装载层（FROM 源视图 TO t_mid_detail）

**模式**：标准化模板

```sql
-- 模板：装载 CASE_SETTLEMENT 粒子
INSERT INTO t_mid_detail (
  batch_id, particle_type, patient_id, visit_id, case_id,
  stat_date, stat_month, discharge_dept_code, discharge_dept_name,
  los_days, total_cost, death_flag
)
SELECT 
  #{batch_id} as batch_id,
  'CASE_SETTLEMENT' as particle_type,
  mr.patient_id, mr.visit_no, CONCAT(mr.patient_id, '#', mr.visit_no),
  mr.discharge_date, DATE_FORMAT(mr.discharge_date, '%Y-%m'),
  mr.discharge_dept_code, mr.discharge_dept_name,
  DATEDIFF(mr.discharge_date, mr.admission_date) as los_days,
  mr.total_charges as total_cost,
  CASE WHEN mr.discharge_type = '死亡' THEN 1 ELSE 0 END as death_flag
FROM I_MR_HOME_PAGE mr
WHERE mr.discharge_date BETWEEN #{start_date} AND #{end_date};
```

**关键点**:
- 源视图直接映射到 t_mid_detail 各字段
- 衍生字段在装载时计算（如 los_days、death_flag）
- 使用 batch_id 追踪数据批次

### 4.2 聚合层（FROM t_mid_detail TO 指标项值）

**模式**：按映射规则自动生成聚合 SQL

```sql
-- 示例：指标项 a0050 (肺炎病种例数)
-- 映射配置:
--   particle_type: CASE_DIAG
--   particle_filter: diag_code LIKE 'J13%' OR diag_code LIKE 'J14%' ...
--   agg_func: COUNT
--   distinct_key: patient_id, visit_id
--   group_by_fields: ["discharge_dept_code", "stat_month"]

SELECT 
  stat_month,
  discharge_dept_code,
  COUNT(DISTINCT CONCAT(patient_id, '#', visit_id)) as item_value
FROM t_mid_detail
WHERE batch_id = #{batch_id}
  AND particle_type = 'CASE_DIAG'
  AND (diag_code LIKE 'J13%' OR diag_code LIKE 'J14%' OR diag_code LIKE 'J15%' OR diag_code LIKE 'J18%')
GROUP BY stat_month, discharge_dept_code;
```

### 4.3 指标层（FROM 指标项 TO 指标值，表达式计算）

保持现有逻辑不变，继续使用 `t_indicator` 中的 expression。

```sql
-- 示例：指标 10.3.2 (肺炎平均住院日)
-- 表达式: a0052 / a0050
-- a0052: 出院患者占用总床日数
-- a0050: 肺炎病种例数

SELECT 
  a.item_value / b.item_value as metric_value
FROM t_indicator_item_result a
  JOIN t_indicator_item_result b ON a.time_value = b.time_value AND a.dept_code = b.dept_code
WHERE a.item_code = 'a0052' AND b.item_code = 'a0050';
```

---

## 五、三份核心文档对应关系

### 5.1 第一份：指标项→最小粒子拆解表

**文件**: 评审标准全章节指标字段映射完整版.docx (已有)

**应补充内容**:
每行指标项增加列:
- `particle_code`: 粒子编码
- `particle_type`: 粒子类型
- `particle_filter_condition`: 粒子过滤条件
- `distinct_key`: 去重键
- `time_field`: 时间字段
- `agg_func`: 聚合函数

**示例行**:
| 指标 | 指标项 | 分子 | 分母 | 粒子代码 | 粒子类型 | 过滤条件 | 去重键 | 时间字段 | 聚合函数 |
|---|---|---|---|---|---|---|---|---|---|
| 1.4.7.1 出院患者平均住院日 | a0050 肺炎病种例数 | 肺炎出院患者 | - | CASE_DIAG | ICD-10 LIKE 'J13%' OR ... | patient_id + visit_id | discharge_date | COUNT_DISTINCT |
| | a0052 住院日总数 | 住院日之和 | - | CASE_SETTLEMENT | los_days > 0 | patient_id + visit_id | discharge_date | SUM |

### 5.2 第二份：最小粒子清单

**新增文件**: 评审指标最小粒子清单.docx

**表格内容**:
| 粒子代码 | 粒子名称 | 粒子类型 | 业务对象 | 粒度 | 唯一键 | 源视图 | 时间字段 | 核心字段 | 支持指标 | 备注 |
|---|---|---|---|---|---|---|---|---|---|---|
| CASE_SETTLEMENT | 病例事实 | INPATIENT | 出院病例 | 一行一出院 | patient_id+visit_no | I_MR_HOME_PAGE | discharge_date | los_days, total_cost, death_flag | 例数、平均住院日、费用、病死率... | 核心粒子，支持全量聚合 |
| CASE_DIAG | 诊断编码 | INPATIENT | 诊断记录 | 一行一诊断 | patient_id+visit_no+diag_code | I_MR_DIAGNOSIS | discharge_date | diag_code, diag_type | 病种命中、并发症、DRG | 用于病种过滤 |
| CASE_OP | 手术事件 | INPATIENT | 手术记录 | 一行一手术 | patient_id+visit_no+op_seq | I_MR_OPERATION | discharge_date | op_code, op_category | 手术统计、四级手术占比 | 可选扩展 |

### 5.3 第三份：中间表计算 SQL 规范

**新增文件**: 评审指标中间表计算SQL.md

**两层 SQL 规范**:

#### 粒子装载 SQL (MID_DETAIL 初始化)

- 文件名: `load_particle_[particle_type].sql`
- 模板: 标准化源视图抽取 → 关联附表 → 衍生字段计算
- 目录: `sql/particle_loaders/`

#### 指标项聚合 SQL (映射表驱动生成)

- 文件名: `agg_item_[item_code].sql`
- 模板: 按映射规则从 t_mid_detail 聚合
- 目录: `sql/item_aggregators/`
- 生成工具: Python 脚本基于 t_item_particle_mapping 元数据生成

---

## 六、实施路线图（分阶段）

### Phase 1: 基础设施搭建（1-2周）
- [ ] 创建 t_mid_detail 表结构
- [ ] 创建 t_agg_rule 和 t_item_particle_mapping 表
- [ ] 编写粒子装载 SQL 模板（CASE_SETTLEMENT、CASE_DIAG）

### Phase 2: 代表性样本验证（1周）
选择 1 个病种（如肺炎）的 4 件套做双算验证:
- [ ] 源视图直算 SQL
- [ ] 粒子层 + 聚合 SQL
- 对比结果是否一致

### Phase 3: 文档与 SQL 生成（2周）
- [ ] 补充完善"指标项→粒子拆解表"的粒子字段列
- [ ] 输出"最小粒子清单"文档
- [ ] 生成 Phase 1 代表性指标的计算 SQL

### Phase 4: 全量落地（3-4周）
- [ ] 批量装载全量粒子（所有章节病种、技术、指标）
- [ ] 生成全量聚合规则和映射数据
- [ ] 验证全量指标计算结果

---

## 七、关键设计决策

### 7.1 为什么是单表方案？
- **灵活性**：可后续按业务需要拆分成多表
- **简化运维**：统一的批处理、备份、清理逻辑
- **快速验证**：前期快速铺开，无需频繁改表结构

### 7.2 为什么要分离聚合层？
- **复用性**：多个指标项可复用同一聚合规则
- **可维护性**：聚合逻辑集中在 t_agg_rule，易修改
- **可追踪性**：清晰的数据血缘：源视图 → 粒子 → 聚合 → 指标

### 7.3 衍生字段在装载时计算的好处
- 避免聚合层重复计算（如 los_days）
- 保证一致性（每个粒子的 los_days 计算方式统一）
- 提高聚合性能（直接 SUM，无需窗口函数）

---

## 八、与现有 MID_TABLE 基础设施的异同

### 相同之处
- 都是元数据驱动的 ETL 框架
- 都使用规则表 + 执行日志 + 存储过程
- 都支持批处理和参数化

### 不同之处
- **MID_TABLE**: 列级衍生规则 + 列级执行日志（细粒度）
- **聚合层**: 聚合级规则 + 聚合级执行日志（粗粒度）

### 建议
可参考 MID_TABLE_RULESET / MID_TABLE_COLUMN_RULE 的结构和执行逻辑，
为 t_agg_rule / t_item_particle_mapping 设计对应的执行存储过程。

---

## 九、后续扩展方向

1. **多源适配**：支持 HIS/EMR/PACS/LIS 等不同系统的源视图映射
2. **质量监控**：对粒子层数据质量进行自动检测（缺失、重复、异常）
3. **动态阈值**：基于历史数据自动调整指标预警阈值
4. **对标分析**：与同类医院、区域平均值进行自动对标
5. **规则引擎**：从 Aviator 或 Drools 集成，使用动态规则语言定义聚合

---

## 十、核心成果物清单

| 序号 | 成果物 | 格式 | 用途 | 状态 |
|---|---|---|---|---|
| 1 | 评审标准全章节指标字段映射完整版 | DOCX | 指标→粒子映射 | ✅ 已有 |
| 2 | 评审指标最小粒子清单 | DOCX | 粒子定义和用途 | 📝 待生成 |
| 3 | 评审指标中间表计算SQL | MD/SQL | 装载与聚合 SQL | 📝 待生成 |
| 4 | t_mid_detail 表结构定义 | SQL | 中间表建表脚本 | 📝 待生成 |
| 5 | t_agg_rule 初始化数据 | SQL | 聚合规则元数据 | 📝 待生成 |
| 6 | 粒子装载 SQL 模板库 | SQL | 各粒子类型的装载 | 📝 待生成 |
| 7 | 指标聚合 SQL 生成器 | Python | 自动生成聚合 SQL | 📝 待生成 |

